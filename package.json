{
  "name": "gatelane",
  "version": "0.0.0-dev",
  "description": "The promotion gate for AI agents — so the next Rehberger demo doesn't ship.",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22",
    "pnpm": ">=10"
  },
  "packageManager": "pnpm@10.0.0",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.build.json --noEmit",
    "lint": "echo 'lint placeholder'"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240821.0",
    "@types/node": "^26.4.0",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}