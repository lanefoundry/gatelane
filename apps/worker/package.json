{
  "name": "@gatelane/worker",
  "version": "0.0.0-dev",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc -b",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "dependencies": {
    "@gatelane/shared": "workspace:*",
    "@gatelane/engine": "workspace:*",
    "@lanefoundry/source-prod-slice": "workspace:*",
    "hono": "^4.5.0"
  }
}
